export type OutlineItem = {
  id: string;
  htmlContent: string;
};

export type OutlineData = {
  topic: string;
  slideCount: number;
  language: string;
  model: string;
  targetAge: string;
  learningObjective: string;
};
