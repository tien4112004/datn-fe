/**
 * @deprecated Legacy theme format. Use SlideTheme from @aiprimary/core instead.
 * This interface is maintained for backward compatibility with applyPresetTheme.
 */
export interface PresetTheme {
  background: string;
  fontColor: string;
  borderColor: string;
  fontname: string;
  colors: string[];
}
