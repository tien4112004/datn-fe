# Quickstart for Period Detail View

This document will contain instructions on how to run and test the Period Detail View feature once it is implemented.

## Running the feature

1.  Start the development server.
2.  Navigate to the Period Detail View page at `/periods/:id` (where `:id` is the ID of a period).